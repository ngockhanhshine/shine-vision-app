// popup-config.js
const POPUP_CONFIG = {
    // 1. Đường dẫn ảnh Popup (đặt file popup.png chung folder hoặc đường dẫn link ảnh)
    imageSrc: "popup.png",

    // 2. Link chuyển hướng khi người dùng bấm vào ảnh (để trống "" nếu không muốn gắn link)
    targetLink: "https://shinevisioncenter.com",

    // 3. Mở link ở tab mới (true) hoặc tab hiện tại (false)
    openInNewTab: true
};